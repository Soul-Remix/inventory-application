<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../stylesheets/style.css">
    <script src="../javascripts/script.js" defer></script>
    <title>
        <%= title %>
    </title>
</head>

<body>
    <%- include('./partials/nav.ejs') %>
        <main>
            <div class="margin-bottom">
                <h2>
                    <%= product.title %>
                </h2>
                <div class="flex-around">
                    <a href=<%=product.url %>/update class="update-link">Update Product</a>
                    <a href=<%=product.url %>/delete class="update-link">Delete Product</a>
                </div>
            </div>
            <div class="flex-around">
                <img src=<%=product.image%> alt=<%= product.title %> class="card-img">
                    <div>
                        <p class="para-50">
                            <%= product.description %>
                        </p>
                        <p class="price">Price: <%= product.price %>
                        </p>
                        <p class="count">In Stock <%= product.inStock %>
                        </p>
                        <p class="category">Category: <%= product.category.name %>
                        </p>
                    </div>
            </div>
        </main>
</body>

</html>